\section{Method}
\label{sec:method}

Let $f : \R^n \to \R$ be the objective. We seek
%
\begin{equation}
  \label{eq:argmin}
  x^\star = \argmin_{x \in \R^n}\, f(x).
\end{equation}
%
\Cref{eq:argmin} is the canonical form; instantiate $f$ for your setting.
